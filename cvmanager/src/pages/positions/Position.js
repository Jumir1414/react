import React from "react";
import { Container } from "react-bootstrap";
import HeaderBar from "../../component/HeaderBar";

const Position = () => {
  return (
    <Container fluid>
      <HeaderBar path={"createposition"} header={"Position"} />
    </Container>
  );
};

export default Position;
